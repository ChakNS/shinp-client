/**
 * @description 表单默认属性
 **/

export const defaultFormAttrs = Object.freeze({
  inline: true,
  size: 'small',
  labelPosition: 'right',
  labelWidth: '80px',
})
export const defaultRowAttrs = Object.freeze({})
